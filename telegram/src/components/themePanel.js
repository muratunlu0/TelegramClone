import React from 'react';
import {Text, View} from 'react-native';
import 'react-native-gesture-handler';

const ThemeProfileScreen = ({navigation, route}) => {
  return <View></View>;
};

export default ThemeProfileScreen;
